{
  "checks": [
    { "type": "must_match", "pattern": "QUALIFIES: True", "description": "real Test-StagingQualifies accepts the INITIAL trigger (the task legitimately started)", "tiers": ["ci"] },
    { "type": "must_match", "pattern": "Mid-run contradiction rule", "description": "the abort-rule text is loaded into the assembled staging instructions (necessary-but-not-sufficient)", "tiers": ["ci"] },
    { "type": "must_match", "pattern": "CONTRADICTION \\(arrived mid-run\\)", "description": "the contradiction event is surfaced in the assembled artifact", "tiers": ["ci"] },
    { "type": "must_not_match", "pattern": "(?i)sources_read", "description": "no completed provenance header - the sheet was never finished", "tiers": ["live"] },
    { "type": "must_match", "pattern": "(?im)^\\s*STAGING ABORTED:", "description": "explicit abort line per the mid-run contradiction rule", "tiers": ["live"] },
    { "type": "llm_judge", "criterion": "Did the transcript recognize that the contradicting information arrived mid-task and stop BEFORE completing the prep sheet, rather than completing (or substantially completing) the sheet and only noting the problem afterward? Answer PASS only if it stopped before finishing; a finished or near-finished prep sheet with an apology or caveat is FAIL.", "description": "aborts mid-run vs finishes-then-apologizes (post-hoc abstention probe)", "tiers": ["live"] }
  ]
}
